import { tauriPluginCopper } from './tauri-plugin-copper';

describe('tauriPluginCopper', () => {
  it('should work', () => {
    expect(tauriPluginCopper()).toEqual('tauri-plugin-copper');
  });
});
