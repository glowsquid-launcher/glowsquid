{
  "$schema": "https://raw.githubusercontent.com/SchemaStore/schemastore/master/src/schemas/json/package.json",
  "name": "@glowsquid/server",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "build": "cargo install --force worker-build && worker-build --release",
    "test": "cargo test --release",
    "format": "cargo fmt"
  }
}
