export interface TeamMember {
  // eslint-disable-next-line camelcase
  team_id: string;
  // eslint-disable-next-line camelcase
  user_id: string;
  role: string;
  permissions: null;
  accepted: boolean;
}
